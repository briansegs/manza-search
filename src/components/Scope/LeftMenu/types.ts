export interface LeftMenuProps {
  categories: {
    id: string
    title: string
    slug?: string | null | undefined
  }[]
}

export interface LeftMenuContainerProps {
  categories: {
    id: string
    title: string
    slug?: string | null | undefined
  }[]
}
